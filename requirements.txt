google-adk
litellm
python-dotenv
# mcp 2.x restructured internals (mcp.shared.session moved) in a way that
# breaks google-adk 2.6.3's MCPToolset import. Pin per ADK's own "mcp" extra.
mcp>=1.24,<2
pyyaml
pywinauto
pywin32
# screen-perception's perception_capture_screenshot/perception_wait_for_visual_change:
# pure-Python, no system binary, unlike the catalog's named DXCam/
# Windows.Graphics.Capture — see orbit/mcp_servers/perception_tools.py's
# module docstring for the OCR/vision-tier libraries deliberately NOT added.
mss
PySide6
# Voice, both directions, on one key: Deepgram Nova-3 streaming STT over
# WebSocket (gui/voice.py) and Deepgram Aura TTS (gui/speech.py). The `speak`
# client ships in the same SDK, so speech output added no new dependency.
# sounddevice does mic capture AND playback; numpy computes the RMS that
# drives the orb.
deepgram-sdk
sounddevice
numpy
# httpx is a litellm dependency, listed explicitly because orbit/ack.py uses it
# directly and on purpose: the acknowledgement track runs in the GUI process,
# where `import litellm` costs 8.7s that a one-second reply cannot afford.
httpx
pytest
pytest-asyncio
